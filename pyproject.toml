[project]
name = "wolke"
version = "2.0.1"
description = "Web-Oriented Layout for Knowledge Exploration — metadata dashboard that feeds NumPy to BLITZ."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
dependencies = [
    "dash",
    "dash-bootstrap-components",
    "dash-mantine-components==0.12.1",
    "eventlet",
    "flask",
    "flask-socketio",
    "gunicorn",
    "numpy",
    "pandas",
    "pillow",
    "plotly",
]

[project.optional-dependencies]
dev = [
    "pyinstaller",
]
